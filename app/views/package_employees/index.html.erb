<h1>Listing package_employees</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @package_employees.each do |package_employee| %>
  <tr>
    <td><%= link_to 'Show', package_employee %></td>
    <td><%= link_to 'Edit', edit_package_employee_path(package_employee) %></td>
    <td><%= link_to 'Destroy', package_employee, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Package employee', new_package_employee_path %>
